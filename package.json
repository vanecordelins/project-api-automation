{
  "name": "servertest-api-tests-full",
  "scripts": {
    "test": "npx cucumber-js --format json:cucumber_report.json --publish-quiet",
    "report": "node generate-report.js"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^10.0.0",
    "@faker-js/faker": "^8.0.2",
    "cucumber-html-reporter": "^6.0.0",
    "pactum": "^3.4.0"
  }
}

